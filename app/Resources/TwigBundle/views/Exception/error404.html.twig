{% extends 'base.html.twig' %}
{% block title %}404 - VedoCompro.it - Acquista e vendi con un video!{% endblock %}

{% block body %}
    {% include 'template/header.html.twig' %}
    <div class="container scrollbar"">
        <div class="row" style="margin-top:200px">
            <div class="col-lg-12">
                <h1 class="text-center" style="font-size: 100px">404!</h1>
                <h3 class="text-center">Ops! Non riesco a trovare la pagina!</h3>

                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    {# ... #}
                {% endif %}
                <br><br>
                <p>
                    Potrebbe trattarsi di un nostro errore, oppure hai digitato un'indirizzo non valido.
                    Ti consigliamo di riporvare o controllare il link da cui provieni, la pagina potrebbe essere stata rimossa,
                    Prova a <a href="{{ path('homepage') }}">tornare alla homepage.</a>.
                </p>
            </div>
        </div>
    </div>
    {% include 'template/footer.html.twig' %}
{% endblock %}