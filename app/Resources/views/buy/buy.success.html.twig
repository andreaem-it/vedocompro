{% extends 'base.html.twig' %}
{% block body %}
{% include 'template/header.html.twig' %}
<div class="wrapper">
    <div class="container-fluid scrollbar"">
        <div class="row">
            <div class="col-sm-12 register-form text-center" style="max-width:500px">
                <br>
                <div class="stoppable mx-auto d-block">
                    <canvas style="display: none" width="400" height="300"></canvas>
                    <img style="display: block" src="{{ asset('img/animated-check.gif') }}"/>
                </div>
                <br>
                <h1 style="font-size:4em">Grazie!</h1>
                <br><br><br>
                <h6>Il tuo acquisto è andato a buon fine.</h6>
                <p><small>{{ credits }} Crediti sono stati aggiunti al tuo account, puoi utilizzarli subito per promuovere le tue inserzioni.</small></p>
                <br><br>
                <a class="btn btn-success" href="{{ url('promote') }}">Promuovi</a>
            </div>
        </div>
    </div>
</div>
    <style>
        .stoppable {
            width: 100px;
            height: 100px;
        }
        .stoppable canvas, .stoppable img {
            width: 100%;
            height: 100%;
        }
    </style>
    <script>
        var c = $(".stoppable canvas")[0];
        var w = c.width;
        var h = c.height;
        var img = $(".stoppable img")[0];
        setTimeout(function () {
            c.getContext('2d').drawImage(img, 0, 0, w, h);
            $(img).hide();
            $(c).show();
        },1000);
    </script>
    {% include 'template/footer.html.twig' %}
{% endblock %}