{% extends 'admin/base.html.twig' %}

{% block title %}VedoCompro.it - ADMIN - Video{% endblock %}

{% block body %}

    <section class="content-header">
        <h1>
            Video
            <small>Moderazione</small>
        </h1>
        <ol class="breadcrumb">
            <li class="active"><a href="#"><i class="fa fa-dashboard"></i> Dashboard</a></li>
            <li>Video</li>
        </ol>
    </section>

    <div class="content container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="box">
                    <div class="box-body table-responsive no-padding">
                        <table class="table table-hover">
                            <tbody>
                            <tr>
                                <th>ID</th>
                                <th>Nome</th>
                            </tr>
                            {% for video in videos %}
                                {% set ad = functions.getAd(video.aid) %}
                                {% if functions.checkExt(ad.video) == 1 %}
                                <tr>
                                    <td>{{ video.id }}</td>
                                    <td><a href="{{ url("admin_vedi_inserzioni",{'id':ad.id}) }}">{{ ad.name }}</a></td>
                                </tr>
                                {% endif %}
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}