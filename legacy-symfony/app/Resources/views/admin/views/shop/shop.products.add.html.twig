{% extends 'admin/base.html.twig' %}

{% block title %}VedoCompro.it - ADMIN - Shop{% endblock %}

{% block body %}
    <!-- Content Wrapper. Contains page content -->

    <!-- Content Header (Page header) -->
    <section class="content-header mb-3 mt-">
        <h1>
            Shop
            <small>Nuovo Prodotto</small>
        </h1>
        <ol class="breadcrumb p-0">

        </ol>
    </section>

    <!-- Main content -->
    <section class="content container-fluid">
        <div class="row">
            <div class="col-12 col-lg-offset-10">

            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <div class="box">
                    <div class="box-body">
                        {% form_theme form 'bootstrap_4_layout.html.twig' %}
                        {{ form_start(form) }}
                        <div class="row">
                            <div class="col-sm-6">
                                {{ form_row(form.name) }}
                            </div>
                            <div class="col-sm-3">
                                {{ form_row(form.price) }}
                            </div>
                            <div class="col-sm-2">
                                {{ form_row(form.inStock) }}
                            </div>
                            <div class="col-sm-1">
                                <div class="checkbox-holder">
                                    {{ form_row(form.isActive) }}
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-sm-6">
                                {{ form_row(form.category) }}
                            </div>
                            <div class="col-sm-6">
                                {{ form_row(form.video) }}
                                {{ form_row(form.pics) }}
                            </div>
                        </div>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>

        </div>
    </section>
    <!-- /.content -->
    <!-- /.content-wrapper -->
    <style>

        .checkbox-holder {
            margin-top: 40px;
        }
    </style>
{% endblock %}